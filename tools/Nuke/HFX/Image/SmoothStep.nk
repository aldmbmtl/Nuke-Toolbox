set cut_paste_input [stack 0]
version 6.3 v4
push $cut_paste_input
Expression {
 expr0 "smoothstep(0, 1, r)"
 expr1 "smoothstep(0, 1, g)"
 expr2 "smoothstep(0, 1, b)"
 expr3 "smoothstep(0, 1, a)"
 name Smoothstep
 selected true
 xpos -1
 ypos -168
}

