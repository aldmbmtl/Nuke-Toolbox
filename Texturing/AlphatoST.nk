set cut_paste_input [stack 0]
version 9.0 v8
push $cut_paste_input
Shuffle {
 name Shuffle133
 selected true
 xpos -2680
 ypos -10
}
Expression {
 expr0 "r > r(x+1, y) ? -r : r"
 expr1 "g > g(x, y+1) ? -g : g"
 expr2 0
 name Expression8
 selected true
 xpos -2680
 ypos 38
}
Multiply {
 channels {rgba.red rgba.green -rgba.blue}
 name Multiply3
 selected true
 xpos -2680
 ypos 80
}
